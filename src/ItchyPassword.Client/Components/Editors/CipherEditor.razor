@using ItchyPassword.Client.Services
@inject ICryptoService Crypto

<div class="alert warn">
    Cipher editing is not yet implemented.
</div>

<div class="form-group">
    <label>Metadata</label>
    <textarea class="input" rows="5" @bind="metadataJson"></textarea>
</div>

@code {
    [Parameter]
    public required VaultItem Item { get; init; }

    [Parameter]
    public string MasterKey { get; set; } = "";

    private string metadataJson = "{}";

    protected override void OnInitialized()
    {
        // Simple serialization for now
        // metadataJson = System.Text.Json.JsonSerializer.Serialize(Item.Parameters);
    }
}
